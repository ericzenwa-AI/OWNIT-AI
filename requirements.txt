# Install with:  pip install -r requirements.txt

# API
fastapi==0.141.1
uvicorn==0.52.1
pydantic==2.13.4
python-multipart==0.0.32   # needed for file uploads (/upload-pdf)

# Claude
anthropic==0.120.2
python-dotenv==1.2.2       # loads ANTHROPIC_API_KEY from backend/.env

# Data
pypdf==6.14.2              # PDF text extraction
PyYAML==6.0.3              # reads data/skills.yaml

# Tests
pytest==9.1.1
